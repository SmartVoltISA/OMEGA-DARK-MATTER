# Ω-DM-001 — RESULT-001

**Status:** NOT EXECUTED / RESULT PENDING
**Classification:** INVALID for scientific conclusion until the executable is run and its output is independently checked.

## What is established now

- The preregistered synthetic protocol exists.
- A stdlib-only executable exists at `run_synthetic.py`.
- The generator contains an explicitly injected hidden relational contribution.
- Visible-only, relation-aware, null, and topology-permutation paths are defined.
- A held-out split is defined.

## What is deliberately NOT claimed

No numerical PASS/FAIL result is claimed yet. No evidence about physical dark matter, gravity, galaxies, lensing, or cosmology is claimed.

## Required execution record

When executed, append:

- exact commit SHA;
- runtime/Python version;
- stdout or machine-readable summary;
- recovered `A` and `lambda`;
- held-out RMSE for M0 and M1;
- null-control distribution;
- permutation-control distribution;
- PASS/FAIL/INVALID decision;
- independent reproduction record.

The experiment is considered complete only after execution and result verification.
